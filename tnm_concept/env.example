# ===================================
# TNM Concept - Environment Variables
# ===================================
# Copy this file to .env and fill in your actual values
# Never commit .env with real secrets to version control

# -----------------------------------
# Supabase Configuration (Required)
# -----------------------------------
# Get these from: https://supabase.com/dashboard/project/YOUR_PROJECT/settings/api
VITE_SUPABASE_URL="https://your-project-id.supabase.co"
VITE_SUPABASE_ANON_KEY="your-anon-key-here"
VITE_SUPABASE_PROJECT_ID="your-project-id"
VITE_SUPABASE_PUBLISHABLE_KEY="your-anon-key-here"

# -----------------------------------
# MT5 Service Configuration (Required)
# -----------------------------------
# MT5 Service Base URL
# 
# LOCAL DEVELOPMENT OPTIONS:
# 1. ngrok tunnel (recommended for cross-machine access):
#    VITE_MT5_SERVICE_URL="https://your-ngrok-subdomain.ngrok-free.app"
#    See: docs/stories/1-5-ngrok-tunnel-for-supabase-edge-function-testing.md
#
# 2. Direct local network (Windows host):
#    VITE_MT5_SERVICE_URL="http://vms.tnm.local:8000"
#    OR
#    VITE_MT5_SERVICE_URL="http://10.4.0.180:8000"
#
# PRODUCTION:
#    VITE_MT5_SERVICE_URL="https://mt5.yourdomain.com"
#    (Requires publicly accessible MT5 service deployment)
VITE_MT5_SERVICE_URL="http://localhost:8000"

# MT5 WebSocket URL (for Story 6.1 - Future WebSocket implementation)
# Replace 'http' with 'ws' or 'https' with 'wss'
# Example: "wss://your-ngrok-subdomain.ngrok-free.app"
VITE_MT5_SERVICE_WS="ws://localhost:8000"

# -----------------------------------
# Feature Flags
# -----------------------------------
# Enable Supabase Realtime subscriptions for live data updates
# Used in Stories 5.3 (LinkedAccountsList) and 5.4 (AIHub)
# Set to "false" to disable Realtime and use polling only
VITE_ENABLE_REALTIME="true"

# Enable MT5 WebSocket connections (Story 6.1)
# Set to "false" until WebSocket implementation is complete
VITE_ENABLE_MT5_WEBSOCKET="false"

# -----------------------------------
# Optional: Development Overrides
# -----------------------------------
# Uncomment to override default behaviors in development

# API request timeout (milliseconds)
# VITE_API_TIMEOUT="5000"

# Enable debug logging
# VITE_DEBUG="true"

# -----------------------------------
# Notes for Developers
# -----------------------------------
# 1. NEVER commit .env with real secrets
# 2. Keep .env.local for local overrides (gitignored)
# 3. Sync environment with backend (.env in mt5-service)
# 4. Run `npm run build` to verify all required vars are present
# 5. See docs/LOCAL-DEVELOPMENT-GUIDE.md for complete setup instructions
